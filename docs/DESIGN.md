# DESIGN.md G-Chat 디자인 시스템

> DESIGN_DELTA.md 반영됨(1.5단계, 2026-09-06). 라디우스 6단(xs 6 / sm 10 / md 12 / lg 16 / xl 20 / full), 그림자 3단(sm md lg. card 는 sm, float 는 lg 별칭),
> 모션 이징 4종(out inOut drawer spring)과 duration 5종(press 120 / fast 160 / pop 180 / dur 280 / sheet 360), Button secondary 의 ring-inset 은
> DESIGN_DELTA.md 가 우선한다. 아래 본문의 모서리 절과 그림자 절과 모션 절의 옛 값은 DESIGN_DELTA.md 로 대체되었다.

작성일 2026년 9월 6일. 공공시설 AI 민원 SaaS G-Chat의 프론트엔드 디자인 규칙이다. 동해사이 프로젝트의 DESIGN.md 규율을 이어받되 색과 대상과 화면 종류가 다르므로 여기 적힌 것이 우선한다. 값은 전부 src/tokens.js에 있고 이 문서는 그 값의 역할과 사용 규칙을 적는다. 이 문서와 tokens.js가 다르면 tokens.js를 고치고 이 문서를 맞춘다. 코드에서 hex와 px를 직접 쓰지 않는다.

## 플랫폼

B형 반응형 웹. 320px에서 3840px까지 단일 코드베이스.

두 개의 면이 있다.

- 시민 면. 챗봇 홈과 시설 안내. 모바일 우선. 스마트폰으로 시설 앞에서 열어 보는 상황이 기준이다.
- 관리자 면. 대시보드와 운영 관리. 데스크톱 우선. 1024px 이상이 기준이고 그 아래는 사이드바를 접고 표를 카드로 바꾼다. 768px 미만에서는 읽기만 가능하고 편집 진입을 막지 않되 권장하지 않는다는 배너를 띄운다.

앱이 아니다. 바텀시트 없다. 하단 탭바 없다. 모달은 중앙 다이얼로그, 보조 패널은 우측 드로어다.

## 벤치마크와 톤

네이버 하이퍼클로바X의 첫 화면 구조를 따른다. 흰 배경, 화면 중앙의 큰 입력창 하나, 그 아래 추천 질문, 배경 장식 없음. 대화가 시작되면 입력창이 하단으로 내려가고 대화가 위에서 아래로 쌓인다.

관리자 면은 Linear와 Vercel 대시보드의 밀도를 따른다. 회색 배경 위 흰 카드, 얇은 1px 경계, 숫자는 tabular-nums, 장식 없음.

원칙 세 가지.

- 공공 서비스다. 신뢰가 먼저다. 화려함으로 신뢰를 만들려 하지 않는다. 출처가 보이고 담당자에게 연결되는 길이 항상 보이는 것이 신뢰다.
- 콘텐츠가 주인공이다. 답변 텍스트, 시설 상태, KPI 숫자. UI는 가능한 한 사라진다.
- 사업계획서와 화면이 한 몸이어야 한다. 대시보드 KPI 이름은 사업계획서 4대 가설의 지표 이름과 글자 단위로 같다. 심사위원이 사업계획서를 옆에 두고 화면을 보기 때문이다.

## 색

### 프라이머리

| 역할 | 토큰 | HEX |
|------|------|-----|
| 프라이머리 | primary | #2563EB |
| 프라이머리 hover와 pressed | primary-hover | #1D4ED8 |
| 프라이머리 옅은 배경. 선택, 사용자 말풍선, 배지 | primary-soft | #EFF6FF |
| 프라이머리 옅은 경계 | primary-line | #BFDBFE |
| 프라이머리 진한 텍스트. 옅은 배경 위 글자 | primary-text | #1E40AF |

프라이머리는 행동을 부르는 곳에만 쓴다. 전송 버튼, 주요 CTA, 선택 상태, 링크, 활성 탭 인디케이터, 차트 주 계열. 장식으로 쓰지 않는다.

### 배경

| 역할 | 토큰 | HEX |
|------|------|-----|
| 페이지 배경. 시민 면 전체, 관리자 카드 표면 | page | #FFFFFF |
| 관리자 면 캔버스 배경 | canvas | #F5F7FA |
| 눌린 톤. hover 배경, 입력창 내부 | mute | #F0F2F5 |
| 표 헤더, 코드 배경 | subtle | #F8F9FB |

흰색과 회색은 이 넷만 쓴다. 웜 톤 회색 금지.

### 텍스트

| 역할 | 토큰 | HEX | 대비(흰 배경) |
|------|------|-----|------|
| 주 텍스트 | text-pri | #101010 | 19.6 |
| 보조 텍스트 | text-sec | #3D3F45 | 10.9 |
| 메타 텍스트. 캡션, 날짜, 라벨 | text-meta | #6B6F76 | 5.4 |
| 비활성. placeholder, disabled | text-ter | #9A9EA5 | 2.9 |
| 반전. 프라이머리 위 | text-inverse | #FFFFFF | |

text-ter는 placeholder와 disabled에만 쓴다. 정보를 담은 텍스트에 쓰지 않는다. 대비 4.5 미만이다.

### 경계선

| 역할 | 토큰 | HEX |
|------|------|-----|
| 얇은 구분선. 리스트 행, 섹션 | line-sub | #ECEEF1 |
| 기본 경계. 입력창, 카드 | line-def | #DDE1E6 |
| 강한 경계. 포커스 전 hover | line-strong | #C5CAD1 |

### 시스템 색

관리자 대시보드는 상태 표기가 많아 시스템 색을 시민 면보다 자주 쓴다. 그래도 각 색에 옅은 배경과 진한 텍스트 짝을 두고 그 짝으로만 쓴다.

각 색은 세 값을 짝으로 가진다. **정색**은 점과 아이콘과 차트에만 쓰고 배경으로 채우지 않는다.
**soft** 는 배지와 배너의 연한 tint 배경이고, **text** 는 그 위에 얹는 채도 있는 진한 글자다.

| 역할 | 토큰 | 정색 (HSL) | 흰 배경 | soft | 글자 (HSL) | 글자 대 soft |
|------|------|-----|------|------|------|------|
| 성공. 정상, 자동처리 완료, 승인 | success | #16A34A (142,76%,36%) | 3.30:1 | #E7F8EE | #0A7C3C (146,85%,26%) | 4.81:1 |
| 경고. 유지보수, 검토 대기, 정확도 하락 | warning | #EA8600 (34,100%,46%) | 2.66:1 | #FEF4E4 | #A85C05 (32,94%,34%) | 4.59:1 |
| 위험. 이슈 발생, 실패, 반려 | danger | #E11414 (0,84%,48%) | 4.87:1 | #FEEBEB | #D01818 (0,79%,45%) | 4.78:1 |
| 정보. 인계됨, 안내 | info | #2563EB (221,83%,53%) | 5.17:1 | #E9F0FE | #1D4ED8 (224,76%,48%) | 5.86:1 |

정보 색은 프라이머리와 같다. 별도 파랑을 만들지 않는다.

**8단계(2026-09-07)에서 7단계의 정색 배경 + 흰 글자를 되돌렸다.** 그 조합은 흰 글자 4.5:1 을 강제하고,
그 제약을 맞추려면 네 색을 전부 어둡게 눌러야 한다. 특히 앰버가 갈색(#B85C00)이 되어 화면이 탁해졌다.
배지가 정색 배경을 쓰지 않으면 그 제약이 사라지므로 정색을 다시 채도 최대로 올렸다.
HSL 채도는 넷 다 76% 이상이다. **채도가 낮으면 회색기가 돌아 탁해 보인다. 색을 고를 때 S 값을 먼저 본다.**

정색 위에 흰 글자를 얹는 곳은 둘뿐이고 둘 다 배지가 아니다. danger 버튼과 알림 카운트 인디케이터이며
`danger` 정색이 흰 글자 4.87:1 이라 둘 다 AA 를 만족한다. 그래서 danger 정색만 4.5:1 하한을 지킨다.

배지와 필의 neutral 은 mute 배경 + text-sec 다. 상태가 아니라 값 없음 표기다.

### 차트 색

프라이머리 단색 계열 + 회색. 무지개 금지.

| 순서 | 토큰 | HEX (HSL) | 흰 배경 대비 | 사용 |
|------|------|-----|------|------|
| 1 | chart-1 | #2563EB (221,83%,53%) | 5.17:1 | 주 계열. 자동처리, 이번 기간 |
| 2 | chart-2 | #0284C7 (199,97%,39%) | 4.10:1 | 보조 계열. 인계, 지난 기간 |
| 3 | chart-3 | #6366F1 (239,84%,67%) | 4.47:1 | 3계열. 미해결. 파선과 함께 쓴다 |
| 4 | chart-4 | #8A929C (213,8%,58%) | 3.15:1 | 기타 조각, 비교 기준선. **유일한 중립색** |
| 히트맵 1 | chart-heat-1 | #9DBEFB (219,92%,80%) | 1.88:1 | 히트맵 최저 단계 |
| 히트맵 2 | chart-heat-2 | #71A1F9 (219,92%,71%) | 2.57:1 | |
| 히트맵 3 | chart-heat-3 | #4583F7 (219,92%,62%) | 3.59:1 | |
| 히트맵 4 | chart-heat-4 | #2563EB | 5.17:1 | 히트맵 최고 단계. chart-1 과 같은 값 |

8단계(2026-09-07)에서 데이터 3계열을 전부 채도 있는 파랑으로 두고 중립 회색은 하나만 남겼다.
도넛이 파랑과 회색이 섞여 흐렸기 때문이다. 셋은 명도가 아니라 **색상**으로 갈린다(221 / 199 / 239).
히트맵 채도를 83% 에서 92% 로 올려 최저 단계도 회색빛 없이 연한 파랑으로 읽힌다.
알파(primary/20 등)를 흰 면에 겹치면 채도가 빠져 뿌예지므로 단계마다 실색을 둔다.
0 은 데이터 없음이라 색 단계가 아니고 mute 다.

3단계(2026-09-06)에서 재조정했다. 이전 값(chart-2 #93C5FD 1.80:1, chart-3 #DBEAFE 1.22:1)은 2px 선이 흰 배경에서 보이지 않아 WCAG 2.2 1.4.11(비텍스트 대비 3:1)에 미달했다. 데이터 3계열은 전부 3:1 이상이다.

계열 간 상호 대비는 명도만으로는 1.4를 넘기기 어렵다. 흰 배경 3:1 을 만족하는 색은 좁은 명도 구간에 몰리기 때문이다. 그래서 chart-3 을 파랑이 아닌 중립색으로 두어 색상으로 구분되게 했고, 범례 라벨과 파선 패턴을 항상 함께 둔다.

색으로만 뜻을 구분하지 않는다. 범례 라벨과 패턴을 항상 같이 둔다.

### 금지

- 그라데이션 전면 금지. bg-gradient 클래스 금지
- 정의 외 hex 금지. tokens.js 경유만
- 프라이머리 외 채도 있는 색을 장식에 쓰는 것 금지. 시스템 색은 상태 표기에만
- 다크 모드 없음. 이번 범위 밖

## 폰트

Pretendard Variable 단독. fallback 추가 금지.

| Weight | 값 | 사용처 |
|--------|-----|--------|
| ExtraBold | 800 | KPI 숫자 |
| Bold | 700 | 페이지 타이틀, 카드 타이틀, Display, 아바타 이니셜 |
| SemiBold | 600 | 섹션 헤딩, 캡션 라벨, 배지, 버튼, 표 헤더, 탭 |
| Medium | 500 | 네비게이션, 사용자 말풍선 |
| Regular | 400 | 본문, 답변 텍스트, 표 셀, 메타 |

Light 안 쓴다. 관리자 면은 정보 밀도가 높아 300은 읽히지 않는다. 한 화면에 최소 세 weight가 보여야 위계가 선다.

**인접해 놓이는 짝은 최소 200 차이가 난다.** 8단계(2026-09-07)에서 "죄다 얇아서 위계가 안 보인다"는
피드백으로 사다리를 넷으로 벌렸다. 라벨(caption 600) 대 값(kpi 800), 카드 타이틀(h3 700) 대 본문(body 400),
델타(caption 600) 대 보조 문구(meta 400) 다. 400 과 500 만 인접하면 위계가 서지 않는다.

## 타이포그래피

전부 clamp()로 유동. 브레이크포인트에서 점프하지 않는다. tracking과 leading은 크기에 따라 달라진다. 하나의 letter-spacing을 전 크기에 걸지 않는다.

| 역할 | 토큰 | size clamp | weight | tracking | leading |
|------|------|-----------|--------|----------|---------|
| Display. 챗봇 홈 헤드라인 | display | clamp(30px, 2vw + 21px, 42px) | 700 | -0.035em | 1.14 |
| H1 페이지 타이틀 | h1 | clamp(24px, 1vw + 19px, 32px) | 700 | -0.025em | 1.2 |
| H2 섹션 헤딩 | h2 | clamp(18px, 0.5vw + 16px, 22px) | 600 | -0.02em | 1.25 |
| H3 카드 타이틀 | h3 | clamp(17px, 0.3vw + 15.5px, 19px) | 700 | -0.02em | 1.3 |
| KPI 숫자 | kpi | clamp(28px, 1.2vw + 22px, 40px) | 800 | -0.03em | 1.05 |
| Body 답변 본문 | body | clamp(15px, 0.2vw + 14px, 17px) | 400 | -0.01em | 1.7 |
| Body Small. 표 셀, 카드 설명 | body-sm | clamp(13px, 0.15vw + 12.5px, 14px) | 400 | -0.005em | 1.55 |
| Caption. 라벨, 배지 | caption | clamp(11px, 0.1vw + 10.5px, 12px) | 600 | 0 | 1.4 |
| Meta. 출처, 시간 | meta | 12px 고정 | 400 | 0.01em | 1.4 |
| Count. 알림 카운트 전용 | count | 10px 고정 | 700 | 0.01em | 1 |

8단계에서 큰 활자는 크기 하한을 올리고 자간을 좁혀 덩어리감을 줬고(display h1 h3 kpi),
캡션 라벨을 500 에서 600 으로, KPI 숫자를 700 에서 800 으로 올렸다. 폰트는 Pretendard 그대로다.
count 는 알림 인디케이터가 16px 원 안에 들어가야 해서 만든 유일한 고정 10px 이다.

답변 본문 leading 1.7은 고정이다. 한글 장문이 이보다 좁으면 읽기 속도가 떨어진다.

숫자는 항상 tabular-nums. KPI, 표, 시간, 카운트 전부.

## 간격

8pt 기반. 4 8 12 16 20 24 32 40 48 64 80 96.

| 영역 | 값 |
|------|-----|
| 카드 내부 패딩 | 16 (모바일) 20 (데스크톱) 24 (KPI 카드) |
| 카드 간 gap | 12 (모바일) 16 (데스크톱) |
| 섹션 상하 | clamp(32px, 4vw, 64px) |
| 페이지 좌우 패딩 | xs 16, md 24, lg 32, xl 40, 3xl 64 |
| 표 셀 패딩 | 12 16 |
| 대화 메시지 간 | 24 |

## 모서리

| 토큰 | 값 | 사용처 |
|------|-----|--------|
| sm | 6px | 배지, 작은 칩, 표 내부 |
| md | 10px | 입력창, 버튼, 작은 카드 |
| lg | 14px | 카드, 드로어 내부 블록 |
| xl | 20px | 대형 입력창(챗봇), 모달 |
| full | 9999px | 원형 버튼, 추천 칩, 아바타 |

이 다섯 외 임의 값 금지.

## 그림자

동해사이는 그림자를 금지했다. G-Chat 관리자 면은 회색 캔버스 위 흰 카드라 경계만으로도 서지만, 떠 있는 요소(드로어, 모달, 드롭다운, 토스트)는 그림자 없이는 층이 안 보인다. 그래서 두 단계만 허용한다.

| 토큰 | 값 | 사용처 |
|------|-----|--------|
| card | 0 0 0 1px rgba(16,16,16,0.06) | 카드 경계 대용. border 대신 ring |
| float | 0 4px 12px -2px rgba(16,16,16,0.10), 0 12px 32px -8px rgba(16,16,16,0.16) | 드로어, 모달, 드롭다운, 토스트 |

시민 면 챗봇 화면에는 float를 쓰지 않는다. 그림자로 카드를 꾸미지 않는다.

## 브레이크포인트

```
xs 320 / sm 390 / md 768 / lg 1024 / xl 1280 / 2xl 1440 / 3xl 1920 / 4xl 2560 / 5xl 3840
```

브레이크포인트는 구조 변화에만 쓴다. 열 수, 사이드바 유무, 표를 카드로 바꾸는 시점. 크기 변화는 clamp와 minmax로 한다.

### 구조 변화 표

| 구간 | 시민 면 | 관리자 면 |
|------|--------|---------|
| 320~767 | 단일 열. 근거 카드는 답변 아래 스택. 시설 카드 1열 | 사이드바 숨김, 햄버거로 드로어. 표는 카드 리스트로. KPI 2열 |
| 768~1023 | 시설 카드 2열. 근거는 여전히 답변 아래 | 사이드바 아이콘 레일 64px. 표 유지, 열 일부 숨김. KPI 2열 |
| 1024~1279 | 대화 [본문 1fr 근거 320px]. 시설 카드 3열 | 사이드바 240px. 표 전체. KPI 4열 |
| 1280~1919 | 대화 [본문 1fr 근거 340px] max 1140 중앙 | 콘텐츠 max 1600 |
| 1920~3840 | 폭을 늘리지 않는다. max 1140 유지. 좌우 여백 커짐. 본문 폰트는 clamp 상한에서 멈춤 | max 1600 유지. KPI 4열 고정. 차트 높이만 clamp로 커짐. 사이드바 240 고정 |

4K에서 콘텐츠를 늘리지 않는다. 늘리면 시선 이동 거리가 커져 오히려 못 읽는다. 여백이 넓어지는 게 정답이다.

## 레이아웃

### 시민 면

- TopNav 높이 56 (모바일) 64 (데스크톱). 흰 배경, 하단 1px line-sub. 좌 로고, 우 시설 안내 / 공지 / 자주 묻는 질문 텍스트 메뉴 + 언어 전환.
- 챗봇 홈 idle: 헤드라인 중앙, 입력창 max 680 중앙, 추천 칩 4개 한 줄(모바일 2×2), 하단 신뢰 문구.
- 챗봇 홈 chat: 좌측 56px 레일(새 대화), 대화 컬럼 max 1140. 답변마다 [본문 1fr | 근거 340px] 한 행. 근거가 없어도 우측 트랙은 예약된다. 입력창은 하단 고정, 본문 열과 같은 폭.
- 대화 진입 시 TopNav는 max-width 제한을 풀고 좌측 정렬로 넓어진다(useChatUi.panelOpen). 동해사이 검증 완료 패턴.

### 관리자 면

- 사이드바 240px 고정, canvas 배경, 우측 1px line-sub. 상단 기관명과 로고, 메뉴 9개, 하단 관리자 프로필.
- 상단바 56px. 좌 페이지 타이틀과 브레드크럼, 우 기간 탭(7일 30일 분기) + 검색 + 알림 + 리포트 다운로드.
- 콘텐츠 max 1600 중앙, 패딩 32.
- 카드는 page 배경 + shadow-card ring. border 없음.

## 컴포넌트 스타일 요약

상세는 COMPONENTS.md. 여기는 원칙.

- 버튼: 높이 40 (기본) 44 (모바일 터치) 32 (표 안 컴팩트). radius md. primary / secondary(line-def) / ghost / danger 네 종. hover는 배경색 변화만. press는 scale(0.97) 허용.
- 입력창: 높이 44, radius md, border line-def, focus 시 border primary + ring 2px primary-line. 배경 page. 관리자 필터 영역 입력은 배경 mute.
- 챗봇 대형 입력창: textarea, min-h 56, max-h 200 자동 확장, radius xl, 우측 원형 전송 버튼 44. 빈 상태는 primary-soft 위 primary 아이콘, 입력 있으면 primary 위 흰 아이콘.
- 말풍선: 사용자는 primary-soft 배경 radius lg 우측 정렬 max 85%. 답변은 배경 없음, 본문 그대로. 답변에 배경 박스를 두지 않는다.
- 배지: caption 600, radius xs, 시스템 색 **soft 배경 + 진한 글자**. neutral 은 mute 배경 + text-sec.
- 표: 헤더 subtle 배경 caption 600 text-meta. 행 hover mute. 행 경계 line-sub. 첫 열 sticky는 표 폭이 넘칠 때만.
- 탭: 하단 2px 인디케이터 primary. 텍스트 500. 활성 text-pri, 비활성 text-meta.
- KPI 카드: 라벨 caption text-meta, 숫자 kpi tabular, 아래 전기간 대비 증감 caption + 방향 아이콘 + 목표선 문구. 목표 미달이면 warning 텍스트.
- 상태 필: soft 배경 + 진한 글자 caption + 6px 정색 점. 정상 success / 유지보수 warning / 이슈 danger / 인계 info.
  점은 장식이라 aria-hidden 이고, 색으로만 뜻을 전하지 않는다는 규칙은 필 안 텍스트 라벨이 지킨다.
- 알림 카운트: 배지가 아니라 인디케이터다. 16px 원(두 자리면 알약), type-count 10px 700,
  danger 정색 배경 + 흰 글자, page 색 2px 링. 벨 아이콘 우상단 밖에 걸쳐 아이콘을 가리지 않는다.
- 아바타: primary 정색 배경 + 흰 글자 700. soft 배경 + 연한 글자는 이니셜이 안 읽힌다.

## 아이콘과 일러스트

아이콘은 lucide-react 단독. 크기 16 20 24 32 48 다섯 단계. 색은 텍스트 토큰만. stroke 1.75 고정.

에이전트는 다른 아이콘 라이브러리를 도입하지 않는다. 필요하면 현호가 승인한다.

일러스트는 unDraw만. 빈 상태, 로그인, 404에만. 색은 primary 단색으로 바꿔 SVG로 public/images/illustrations/에 둔다. 시설 카드와 상세와 대시보드에는 일러스트를 넣지 않는다.

시설 사진은 실사 jpg. 별점 리뷰수 금지. 사진 없는 시설은 mute 배경에 시설 유형 아이콘 24로 채운다.

## z-index 위계

| 층 | 토큰 | 값 | 요소 |
|----|------|-----|------|
| 기본 | base | 0 | 콘텐츠 |
| 떠 있는 콘텐츠 | raised | 10 | 아래로 가기 버튼, sticky 표 헤더 |
| 고정 네비 | nav | 40 | TopNav, 사이드바, 관리자 상단바 |
| 드롭다운 | dropdown | 50 | Select 메뉴, 언어 전환, 사용자 메뉴 |
| 드로어 | drawer | 60 | 우측 상세 드로어, 모바일 사이드바 |
| 모달 | modal | 70 | 확인 다이얼로그, 문서 업로드 |
| 토스트 | toast | 80 | 알림 |

이 일곱 외 임의 z-index 금지. 겹침 문제는 z를 올리는 게 아니라 부모 구조를 고쳐서 푼다.

## 모션

Apple HIG 기준. transform과 opacity만 애니메이션한다. width height top left margin padding을 애니메이션하지 않는다.

### 토큰

| 토큰 | 값 | 사용 |
|------|-----|------|
| duration-fast | 120ms | 색 변화, 아이콘 전환 |
| duration-base | 200ms | 버튼 hover 배경, 탭 인디케이터, 칩 |
| duration-enter | 240ms | 메시지 진입, 카드 진입, 드로어 |
| duration-page | 320ms | 페이지 전환, TopNav 폭 전환 |
| ease-out | cubic-bezier(0.16, 1, 0.3, 1) | 진입, 확장 |
| ease-in-out | cubic-bezier(0.65, 0, 0.35, 1) | 위치 이동 |
| ease-standard | cubic-bezier(0.2, 0, 0, 1) | 색과 opacity |
| spring-press | scale(0.97) 120ms ease-out | 버튼 press |

### 차트 표현 규율 (7단계)

| 요소 | 규칙 |
|------|------|
| 꺾은선 | stroke 2.5px. 파선(미해결) 2px. monotone cubic 으로 눕힌다. 일반 스플라인은 오버슈트로 0 아래 가짜 골짜기를 만든다 |
| 데이터 포인트 | 반지름 3.5 원. 계열색 채움 + page 색 1.5px 테두리. 점 간격이 24px 미만이면(분기 같은 긴 구간) 마커를 그리지 않는다 |
| 면 채움 | 주 계열 하나만. 계열색 단색에 fill-opacity 0.1. 그라데이션 금지는 그대로다 |
| 막대 | 최대 폭 72, 그룹 폭의 78%. radius 4 |
| 도넛 | 링 두께 30, 조각 간격 5px 고정, 시작각 12시. 조각이 하나면 간격 0. stroke-linecap 은 butt 다. 둥근 캡은 양 끝에 STROKE/2 씩 더 그려 간격을 보이게 하려면 조각마다 35px 넘게 잘라야 하고 네 조각이면 원의 30% 가 사라진다 |
| 히트맵 | 단계 색은 chart-heat-1~4. 0 은 mute |
| 진입 | 최초 1회. reduced-motion 이면 전역 규칙이 0.01ms 로 만들어 즉시 최종 상태 |

### 허용 목록

| 효과 | 값 | 대상 |
|------|-----|------|
| 페이지 진입 | opacity 0→1, translateY 8→0, page/ease-out | 라우트 전환 |
| 메시지 진입 | opacity 0→1, translateY -6→0, enter/ease-out | 새 말풍선, 답변 행 |
| 인트로 소멸 | opacity 1→0, translateY 0→-8, enter/ease-out | 챗봇 idle→chat |
| TopNav 폭 전환 | max-width, page/ease-out | 대화 진입 시 헤더 확장. 유일한 레이아웃 속성 예외. 헤더 하나라 비용 감당 가능 |
| 드로어 | translateX 100%→0, enter/ease-out. 배경 opacity 0→1 | 우측 드로어 |
| 모달 | opacity 0→1, scale 0.98→1, enter/ease-out | 다이얼로그 |
| hover 배경 | background-color, base/ease-standard | 버튼, 행, 칩 |
| press | scale 0.97, fast | 버튼 |
| 스켈레톤 | background-color 두 회색 사이, 1.4s ease-in-out 무한 | 답변 대기 바 |
| 아이콘 전환 | opacity + scale 0.75↔1, fast | 복사→체크 |
| 차트 진입 | 막대 scaleY 0→1 transform-origin bottom, 꺾은선 stroke-dashoffset, enter/ease-out. 최초 1회만 | 대시보드 |
| KPI 숫자 | 0→값 카운트업 400ms, 최초 1회만 | 대시보드 |

### 금지

- hover에 scale. 레이아웃 흔들림
- hover에 translateY로 떠오르는 효과. 카드 그리드에서 산만함
- 무한 반복 장식 애니메이션. 스켈레톤 제외
- 자동 스크롤로 사용자가 읽고 있는 위치를 끌어내리는 것. 새 질문 상단 앵커만 허용, 스트리밍 중 바닥 추적 금지
- 스트리밍 중 근거 카드 노출. 답변 끝난 뒤에만
- parallax, 3D, WebGL, backdrop-blur

### 접근성 대응

prefers-reduced-motion: reduce 시 전 애니메이션 0.01ms. 스크롤 behavior auto. 카운트업 없이 최종값 즉시.
prefers-reduced-transparency 시 드로어 배경 딤을 불투명 회색으로.
prefers-contrast: more 시 line-sub를 line-def로, text-meta를 text-sec로 올린다. tokens.js에 contrast 변형을 둔다.

## 접근성

WCAG 2.2 AA.

- 모든 인터랙티브 요소 키보드 도달. 탭 순서는 시각 순서와 같다.
- focus-visible 2px primary offset 2px 전역.
- 터치 타깃 최소 44px 모바일. 데스크톱 표 안 컴팩트 버튼 32는 hover 영역 40 확보.
- 텍스트 대비 4.5 이상. 위 텍스트 표의 대비값 기준. text-ter는 정보 텍스트 금지.
- 상태를 색으로만 전하지 않는다. 필과 배지는 항상 텍스트 라벨 동반.
- 스트리밍 답변 영역 aria-live polite. 스켈레톤 aria-label 답변 생성 중.
- 이미지 alt 필수. 장식 이미지는 alt 빈 문자열.
- 폼 라벨 association 필수. placeholder를 라벨 대용으로 쓰지 않는다.
- 언어 전환 시 html lang 갱신.

## 콘텐츠 규칙

### UI 텍스트

- 명사형 명령으로 통일. 예약 상태 확인, 담당자 연결, 문서 업로드. 동사형과 혼용하지 않는다.
- 가운데점 줄표 이모지 이탤릭 금지.
- 기관명은 하드코딩하지 않는다. VITE_ORG_NAME과 설정 화면 값에서 온다. 화면에 동해시가 박혀 있으면 두 번째 기관에 팔 수 없다.
- 심사용 화면이니 사업계획서 용어를 그대로 쓴다. 민원 자동처리율, 응답 정확도, 담당자 응대시간, 이용자 만족도(NPS). 다른 말로 바꾸지 않는다.

### AI 답변 텍스트 (렌더러 규칙)

백엔드가 무엇을 보내든 프론트가 정제한다. lib/stripMarkdown.js는 동해사이 검증 완료 로직을 그대로 가져온다.

- 제목 기호와 백틱 제거, 별표 불릿을 하이픈 불릿으로, 홑별표 강조를 볼드로 승격, 짝 없는 별표 제거, 스트리밍 중 미완성 볼드 감춤, 볼드 뒤 조사 받침 교정.
- 렌더러는 문단 / 하이픈 불릿 / 번호 목록 / 파이프 표 / 볼드를 그린다. 그 외 마크다운은 지원하지 않는다.
- 답변 톤: 공식 안내 톤. 해요체가 아니라 합니다체. 시설관리공단 안내문과 같은 톤이어야 시민이 공식 안내로 받아들인다. 이 규칙은 백엔드 시스템 프롬프트에 넣어야 하며 API_CONTRACT.md에 적는다.

### 근거 표시

답변 하단 근거 카드는 출처 문서명, 문서 종류(운영 매뉴얼 / 공지 / FAQ / 예약 시스템), 갱신일을 보인다. 요구사항 두 번째(이게 공식 안내가 맞나 싶다)에 대한 답이다. 갱신일이 90일 넘으면 warning 텍스트로 오래된 자료일 수 있음을 붙인다.

### 담당자 연결

모든 답변 액션 바에 담당자 연결 버튼이 항상 있다. 신뢰도가 낮거나 자료가 없을 때만 나오는 게 아니다. 요구사항 세 번째(AI가 못 풀면 바로 사람에게)는 사용자가 판단할 권리를 주는 것이다. 누르면 HandoffCard가 답변 아래 인라인으로 열려 부서 전화와 운영시간, 문의 남기기 폼을 보인다.

## 절대 금지 체크리스트

- [ ] tokens.js 외 hex 직접 입력 없음 (index.html theme-color 예외 1건)
- [ ] TypeScript 문법 없음
- [ ] localStorage sessionStorage 없음
- [ ] 이모지 없음 (jsx json md 전부)
- [ ] hover scale, hover translateY 없음
- [ ] transform opacity 외 속성 애니메이션 없음 (TopNav max-width 예외 1건)
- [ ] 네이티브 select, input type=date 노출 없음
- [ ] 그라데이션 없음
- [ ] 별점 리뷰수 없음
- [ ] 기관명 하드코딩 없음
- [ ] 라이브러리 아이콘 lucide 외 없음
- [ ] radius 5종 외 없음, z-index 7단 외 없음
- [ ] 320~3840 가로 스크롤 0
